# Firebase client SDK config (Firebase Console > Project Settings > General > Your apps > Web app)
VITE_FIREBASE_API_KEY=AIzaSyB0ZXW7siXG9NmRseRDMJX-IvP5ydio_gs
VITE_FIREBASE_AUTH_DOMAIN=crelection-583db.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=crelection-583db
VITE_FIREBASE_STORAGE_BUCKET=crelection-583db.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=689790891212
VITE_FIREBASE_APP_ID=1:689790891212:web:d766d52510144c63667e69

# Base URL of the backend API — leave unset. The client now defaults to the
# relative path "/api", which works both locally (vite proxies it to the
# Express dev server) and on Vercel (same-origin serverless function).
# Only set this if you deploy the API on a different origin than the client.
# VITE_API_BASE_URL=

# Admin token used by the /admin/settings page (must match server ADMIN_TOKEN)
VITE_ADMIN_TOKEN=kitkat19
